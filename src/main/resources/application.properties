# Datos de conexión a MySQL
spring.datasource.url=jdbc:mysql://db:3306/taylorsongs
spring.datasource.username=taylorswift
spring.datasource.password=1989password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA (para que cree las tablas automáticamente)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Para mostrar las consultas SQL en consola (útil para debug)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

